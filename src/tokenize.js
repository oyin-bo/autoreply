// @ts-check

/**
 * @typedef {number} RawToken
 */

/**
 * Tokenizes text into reasonable tokens using SentencePiece logic.
 * @param {string} text
 * @param {RawToken[]} tokens
 */
export function tokenize(text, tokens) {
  //
}